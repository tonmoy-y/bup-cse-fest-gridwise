MISSION: INDEPENDENT FULL-SYSTEM AUDIT, BREAK, FIX, REPEAT

You are taking over an already-built GridWise hackathon project.

I will provide:
1. The official GridWise Problem Statement
2. The official Participant Guide & Evaluation Rubric
3. The official Public Sample Cases JSON
4. The current project/repository
5. The current live deployment URL

IMPORTANT:
The previous developer provided a summary of what was supposedly implemented. Treat that summary ONLY as historical context, NOT as proof that anything is correct.

Your job is to independently verify everything.

Do not assume the existing implementation is correct.
Do not trust existing tests blindly.
Do not optimize for "tests passing"; optimize for actual correctness against the official specification.

==================================================
CURRENT PROJECT HISTORY / CONTEXT
==================================================

The project is a FastAPI-based GridWise smart-campus energy optimization API.

Core pipeline:

operator_notes
→ LLM interpretation
→ deterministic schema/guardrail validation
→ directive normalization/application
→ mathematical 24-hour optimizer
→ independent final schedule replay/validation
→ response

Required API:
GET /health
POST /optimize-energy

Exactly 6 supported directives:
1. solar_reduction
2. minimum_battery_reserve
3. no_charge_window
4. no_discharge_window
5. max_grid_window
6. no_op

The LLM MUST genuinely interpret operator_notes.
Keyword matching alone must never replace the required LLM path.

The optimizer must produce a valid 24-hour schedule satisfying:
- hourly demand
- effective solar
- grid usage
- battery transitions
- battery capacity
- base reserve
- directive reserve
- charge limits
- discharge limits
- no-charge windows
- no-discharge windows
- max-grid windows
- energy balance
- end-of-day battery neutrality

The final output must follow the official API schema exactly.

A previous summary claimed:
- all 10 public samples pass
- 167 tests were added
- hostile QA was performed
- Gemini + Grok failover was added
- deployment issues were fixed
- frontend was added
- the live deployment currently works

These are CLAIMS ONLY.
VERIFY EVERY ONE YOURSELF.

==================================================
YOUR OPERATING MODE
==================================================

Work autonomously.

Do NOT just inspect and report.

Use this continuous loop:

AUDIT
→ RUN
→ BREAK
→ REPRODUCE
→ IDENTIFY ROOT CAUSE
→ FIX
→ ADD REGRESSION TEST
→ RERUN RELATED TESTS
→ RERUN FULL SUITE
→ RERUN PUBLIC SAMPLES
→ TEST LIVE DEPLOYMENT
→ AUDIT AGAIN
→ BREAK AGAIN

Repeat this cycle continuously until you can no longer find a credible bug or weakness.

Finding a bug is NOT enough.
You must fix it.

Do NOT stop after the first successful test run.

Do NOT weaken validation, remove tests, reduce correctness, or loosen constraints just to make failures disappear.

==================================================
PHASE 1 — UNDERSTAND EVERYTHING
==================================================

Before changing code:

- inspect the complete repository
- inspect project structure
- inspect backend
- inspect frontend
- inspect optimizer
- inspect LLM integration
- inspect Gemini provider
- inspect Grok provider
- inspect failover logic
- inspect guardrails
- inspect final validator
- inspect tests
- inspect deployment config
- inspect Docker setup
- inspect README
- inspect environment-variable handling
- inspect routing/static-file configuration
- inspect git state and remote

Read the official three challenge files carefully and use them as the source of truth.

Build an internal understanding of:
- exact request schema
- exact response schema
- exact directive semantics
- exact battery rules
- exact optimization objective
- exact validation rules
- official tolerance
- deployment requirements
- performance requirements
- hidden-test expectations
- security requirements

Do not silently invent challenge behavior.

==================================================
PHASE 2 — LOCAL SYSTEM AUDIT
==================================================

Start the application locally using the real project setup.

Verify:
- startup
- imports
- dependencies
- environment handling
- routing
- frontend serving
- /health
- /optimize-energy

Exercise the system with real requests.

Do not rely only on unit tests.

Use curl / HTTP requests and inspect actual responses.

==================================================
PHASE 3 — LIVE DEPLOYMENT AUDIT
==================================================

Use the supplied live deployment URL.

Test it from outside the local environment.

Verify:
- /
- /health
- /optimize-energy
- frontend loading
- API reachability
- JSON response correctness
- cold start behavior
- repeated requests
- LLM provider availability
- failover behavior where safely testable
- deployment environment variables
- static asset paths
- Vercel/runtime configuration

Compare local and live behavior.

If local works but live fails, debug deployment specifically.

Do not create another deployment.
Do not create another repository.
Keep the existing repository and existing deployment.

==================================================
PHASE 4 — FULL GRIDWISE CORRECTNESS AUDIT
==================================================

Audit every one of the six directives independently.

A) solar_reduction
- percentage remaining vs percentage reduction
- factor conversion
- factor 0
- factor 1
- fractional factor
- time boundaries
- noon / PM / 24-hour time
- one-hour window
- multi-hour window
- only affected hours change
- effective solar is correct
- optimizer actually uses reduced solar

B) minimum_battery_reserve
- absolute reserve
- percentage-of-capacity interpretation
- "at least" semantics
- reserve equal to base minimum
- reserve greater than base minimum
- reserve equal to capacity
- reserve below zero rejection
- reserve above capacity rejection
- reserve applied to battery_energy_after_kwh
- reserve ends after specified window

C) no_charge_window
- charging disabled
- charger unavailable
- charging prohibited
- exact hours
- charge exactly zero in affected hours

D) no_discharge_window
- discharge prohibited
- protection wording
- exact hours
- discharge exactly zero in affected hours

E) max_grid_window
- exact cap extraction
- cap zero
- fractional cap
- cap near demand
- cap satisfied using solar/battery
- cap enforced as a HARD constraint

F) no_op
- realistic irrelevant notes
- administrative distractors
- notes containing energy-related words but no schedule-changing instruction
- applies=false
- directive_type=no_op
- structured_adjustment=null

==================================================
PHASE 5 — LLM ROBUSTNESS / SEMANTIC ATTACKS
==================================================

Test paraphrases extensively.

Use:
- formal wording
- casual wording
- passive voice
- synonyms
- fractions
- percentages
- "80% reduction"
- "20% remains"
- "one fifth remains"
- "between"
- "from X until Y"
- "starting X"
- 12-hour clock
- 24-hour clock
- noon
- midnight
- evening
- ambiguous-looking but still valid expressions

Verify semantic equivalence.

Test 1–3 notes:
- all relevant
- all irrelevant
- mixed relevant/irrelevant
- multiple directives
- overlapping compatible directives

Verify EXACTLY one interpretation entry per note and correct note_index ordering.

==================================================
PHASE 6 — MALFORMED / HOSTILE LLM OUTPUT
==================================================

Mock or simulate provider responses such as:

- malformed JSON
- empty output
- truncated JSON
- markdown-wrapped JSON
- duplicate note_index
- missing note_index
- wrong note order
- wrong directive type
- unsupported directive
- invalid hours
- duplicate hours
- unsorted hours
- factor < 0
- factor > 1
- NaN
- Infinity
- negative reserve
- reserve > capacity
- negative grid cap
- wrong applies value
- no_op with non-null adjustment
- non-no_op with null adjustment

Verify invalid model output NEVER reaches the optimizer.

HTTP 200 from the provider is NOT sufficient.
The result is successful only after deterministic validation passes.

==================================================
PHASE 7 — MULTI-PROVIDER FAILOVER AUDIT
==================================================

The current intended provider architecture is:

LLMProvider
├── Gemini
└── Grok

Expected priority:
Gemini → Grok

Verify:

1. Gemini succeeds → Grok is NOT called.
2. Gemini key fails → next Gemini key.
3. All Gemini keys fail → Grok.
4. 401/403 → move away from invalid key.
5. 429 → bounded retry/fallback.
6. timeout → fallback.
7. 5xx/network failure → fallback.
8. malformed LLM result → reject and fallback where policy allows.
9. guardrail failure → reject and fallback where policy allows.
10. Grok must use the SAME canonical interpretation semantics and guardrails.
11. All providers failing → controlled safe failure.
12. No infinite retries.
13. No unnecessary provider calls.
14. No keyword-only fallback.

Verify keys are never logged or exposed.

Multiple Gemini keys must NOT be treated as a way to bypass project-level quota limitations.

==================================================
PHASE 8 — OPTIMIZER / MATHEMATICAL AUDIT
==================================================

Do NOT assume the optimizer is mathematically correct because public samples pass.

Independently verify:

energy balance for every hour:

grid + solar_used + battery_discharge
=
demand + battery_charge

Verify:
- grid >= 0
- solar_used >= 0
- solar_used <= effective_solar
- battery_kwh >= 0
- charge/discharge rate limits
- capacity
- reserve
- state continuity
- correct action semantics
- no simultaneous charge/discharge
- end-of-day battery = initial battery

Recalculate independently:
- total_grid_kwh
- total_cost_bdt
- peak_grid_kwh

Do not trust the values produced by the optimizer itself.

Test tariff patterns:
- constant
- increasing
- decreasing
- one extreme peak
- repeated equal values
- fractional values
- zero tariff

Verify cost minimization AFTER all hard constraints are applied.

==================================================
PHASE 9 — ADVERSARIAL NUMERIC TESTING
==================================================

Test:
- zero values
- fractional values
- values exactly at limits
- values just below limits
- values just above limits
- very small decimals
- large valid values
- capacity boundaries
- reserve boundaries
- rate boundaries
- solar = 0
- solar > demand
- solar = demand
- demand = 0

Watch especially for floating-point rounding bugs.

Do not silently coerce invalid values into valid values.

==================================================
PHASE 10 — REQUEST VALIDATION
==================================================

Test malformed/invalid input:

- missing fields
- nulls
- wrong types
- wrong array lengths
- 23 hours
- 25 hours
- duplicate hours
- unsorted hours
- hour 24
- negative hours
- decimal hours
- empty notes
- >3 notes
- invalid battery values
- initial energy > capacity
- minimum > capacity
- negative numeric values
- malformed JSON

Invalid input must fail cleanly without crashing.

==================================================
PHASE 11 — STATE / RELIABILITY
==================================================

Test:
- same request repeatedly
- different requests sequentially
- alternating scenarios
- concurrent requests
- repeated requests after provider failure
- repeated deployment requests

Look for:
- shared mutable state
- stale LLM result
- stale directives
- battery state leakage
- scenario mixing
- race conditions
- non-deterministic corruption

==================================================
PHASE 12 — PUBLIC SAMPLE VALIDATION
==================================================

Run ALL 10 official public sample cases.

For EACH case independently verify:
- interpretation
- directive structure
- hours
- numeric values
- 24-hour plan
- energy balance
- solar limits
- battery rules
- directive enforcement
- end-of-day neutrality
- total grid
- total cost
- peak grid

Equivalent optimal schedules are valid.
Do not require byte-for-byte matching with the reference schedule.

==================================================
PHASE 13 — RANDOMIZED / PROPERTY TESTING
==================================================

Generate many feasible synthetic scenarios.

Randomize:
- demand
- solar
- tariff
- battery capacity
- initial energy
- reserve
- charge limit
- discharge limit

Generate compatible directives from all six types.

For every result:
independently replay and validate the plan.

Use boundary-heavy randomized cases.

Also test compatible directive combinations and overlapping windows.

==================================================
PHASE 14 — PERFORMANCE
==================================================

Measure:
- startup time
- /health latency
- normal /optimize-energy latency
- fallback latency
- repeated-request latency
- concurrent behavior

The official challenge has a 30-second per-request ceiling and a p95 latency target.

Look for unnecessary LLM calls, slow retries, slow initialization, and avoidable optimizer overhead.

Do not sacrifice correctness to make a benchmark look better.

==================================================
PHASE 15 — SECURITY / REPOSITORY AUDIT
==================================================

Search the entire repository for:

- API keys
- tokens
- passwords
- .env files
- Authorization headers
- credentials
- secret literals

Also verify no secret is exposed in:
- logs
- API responses
- frontend source
- README
- Docker image

==================================================
PHASE 16 — "NO CLAUDE / ANTHROPIC" REQUIREMENT
==================================================

Do NOT add any reference to:
- Claude
- Anthropic
- Claude Code
- Anthropic API
- AI-generated attribution

Do not put such references in:
- source code
- comments
- README
- documentation
- UI
- package metadata
- commit messages
- test names
- console output
- deployment configuration

Before finishing, search the repository for these terms and remove any unnecessary references that are not required by the underlying environment.

DO NOT fabricate attribution.
DO NOT add your own name.

Git commits should contain normal technical messages only.

==================================================
PHASE 17 — FIX EVERYTHING YOU FIND
==================================================

For every discovered bug:

1. reproduce it
2. identify root cause
3. fix it properly
4. add regression test
5. run targeted tests
6. run full test suite
7. run all 10 public samples
8. retest locally
9. retest live deployment if applicable
10. continue searching for more bugs

Never stop simply because one bug was fixed.

==================================================
PHASE 18 — GIT / DEPLOYMENT
==================================================

Use the EXISTING repository and EXISTING remote.

Do not create a new repository.

Preserve the existing deployment/project.

After verified fixes:
- inspect git diff carefully
- remove debug code
- remove secrets
- review changed files


Do not mention Claude/Anthropic in commit messages.

Do not push unverified changes.

==================================================
REQUIREMENT COMPLETENESS + ZERO-GAP AUDIT
==================================================

IMPORTANT:
Do not assume that "the application works" means the challenge requirements are complete.

You must separately audit the CURRENT implementation against EVERY requirement in the official:
1. Problem Statement
2. Participant Guide & Evaluation Rubric
3. Public Sample Cases specification

Create an internal requirement-by-requirement checklist.

For EVERY requirement, determine:

- REQUIRED by official document?
- PRESENT in current implementation?
- ACTUALLY WORKING?
- CORRECTLY IMPLEMENTED?
- TESTED?
- DEPLOYED?
- DOCUMENTED where required?

Do NOT stop at checking whether files/functions exist.
Verify actual behavior.

If ANY required feature, rule, validation, endpoint behavior, output field, optimizer constraint,
LLM behavior, deployment requirement, security requirement, testing requirement, documentation
requirement, Docker requirement, or performance-related requirement is missing or incorrectly
implemented:

YOU MUST IMPLEMENT/FIX IT YOURSELF.

Do NOT merely report the missing item.

After implementing/fixing it:
- add a regression test
- run related tests
- run the full suite
- rerun all 10 public samples
- rerun adversarial tests
- rerun deployment tests
- audit the requirement again

==================================================
OFFICIAL REQUIREMENT COVERAGE
==================================================

Explicitly verify ALL of these categories:

A. API
- GET /health
- POST /optimize-energy
- exact paths/methods
- exact request schema
- exact response schema
- correct HTTP status behavior
- public accessibility
- JSON behavior

B. LLM
- genuine LLM interpretation of every operator_note
- exactly one interpretation per note
- correct note_index order
- paraphrase robustness
- all six directive types
- correct no_op semantics
- correct numeric/time interpretation
- deterministic validation before optimizer
- no keyword-only replacement

C. SIX DIRECTIVES
Independently verify:
- solar_reduction
- minimum_battery_reserve
- no_charge_window
- no_discharge_window
- max_grid_window
- no_op

For EACH directive verify:
- interpretation
- structured_adjustment
- validation
- normalization
- optimizer application
- final replay enforcement
- edge cases
- combinations with other directives

D. OPTIMIZATION
- effective solar
- grid usage
- battery charge/discharge
- energy balance
- battery capacity
- base reserve
- directive reserve
- charge rate
- discharge rate
- no-charge
- no-discharge
- max-grid
- end-of-day neutrality
- total grid
- total cost
- peak grid
- actual cost minimization after hard constraints

E. VALIDATION
- request validation
- LLM output validation
- final schedule validation
- finite numeric values
- non-negative values
- valid hours
- duplicate prevention
- ordering
- state continuity
- independent replay

F. ROBUSTNESS
- malformed JSON
- invalid request types
- malformed LLM output
- invalid provider responses
- 401/403
- 429
- timeout
- 5xx
- network failure
- repeated requests
- concurrent requests
- provider failover
- state leakage
- boundary values
- floating-point edge cases
- adversarial inputs
- prompt injection

G. DEPLOYMENT
- local startup
- external /health
- external /optimize-energy
- frontend serving
- static assets
- environment variables
- cold starts
- repeated requests
- production LLM connectivity
- Docker fallback
- no secrets baked into deployment

H. DOCUMENTATION / SUBMISSION
Verify every officially required submission item exists and is usable:
- README
- local quickstart
- API documentation
- model/provider information
- environment variable names
- optimizer/solver information
- dependency/credit information
- sample request/response
- Docker fallback instructions
- deployment instructions
- testing instructions
- required video/submission-related material where applicable

==================================================
CONTINUOUS BREAK → FIX LOOP
==================================================

Do not perform this audit only once.

Use this cycle repeatedly:

1. Compare implementation against official requirements.
2. Find missing/incorrect requirements.
3. Find bugs.
4. Find hidden-test weaknesses.
5. Find edge cases.
6. Reproduce the problem.
7. Fix the root cause.
8. Add regression coverage.
9. Run all tests.
10. Run all 10 public samples.
11. Test local deployment.
12. Test live deployment.
13. Audit the requirements again.
14. Attack the newly changed code again.

Continue this cycle until repeated independent audits produce no unresolved
requirement gaps and no reproducible correctness bugs.

==================================================
"TRY TO BREAK IT" STANDARD
==================================================

Be deliberately adversarial.

Assume a hidden judge is trying to make the system fail.

Try:
- unusual but valid inputs
- boundary values
- near-boundary values
- equivalent paraphrases
- misleading wording
- irrelevant distractors
- multiple simultaneous directives
- overlapping windows
- provider failures at different stages
- valid HTTP responses containing invalid semantic content
- frontend/backend routing inconsistencies
- deployment-only failures
- timing/race conditions
- floating-point corner cases
- stale state
- inconsistent totals
- optimizer edge conditions

Do not search only for obvious bugs.

Look for:
- silent wrong answers
- off-by-one errors
- incorrect normalization
- hidden assumptions
- provider-specific inconsistencies
- logic that works only for public samples
- logic that passes tests but violates the official specification
- features that exist but are not actually wired into the execution path

==================================================
NO "GOOD ENOUGH" STOPPING RULE
==================================================

Do not stop because:
- all current tests pass
- all 10 public samples pass
- local deployment works
- live deployment currently works
- the code looks clean

Those are necessary but NOT sufficient.

The system must also survive:
independent requirement audit + adversarial testing + repeated regression testing.

If you discover a required feature that was never implemented, implement it.

If you discover a feature that exists but is incomplete, complete it.

If you discover a feature that works only in one path/provider/environment, make the behavior consistent.

==================================================
FINAL VERIFICATION
==================================================

Before declaring completion, perform one FINAL FRESH AUDIT as if you had never seen the project before.

Do not rely on your previous conclusions.

Re-read the official requirements and independently verify:
- nothing required is missing
- nothing required is incorrectly implemented
- no major edge case remains untested
- no critical failure path remains
- all six directives are fully covered
- failover works
- optimizer remains correct
- final replay remains correct
- API contract remains exact
- local and live deployments work

Only then declare completion.

Your objective is not merely "the code works".

Your objective is:
MAXIMUM REQUIREMENT COVERAGE + MAXIMUM ROBUSTNESS + MINIMUM UNDISCOVERED FAILURE SURFACE.

==================================================
FINAL ACCEPTANCE CONDITION
==================================================

Do NOT declare the system perfect merely because the existing tests pass.

Only finish after performing multiple audit/fix cycles and reaching a point where:

- official requirements are satisfied
- all six directives behave correctly
- semantic paraphrases work
- malformed LLM outputs are safely handled
- provider failover is reliable
- schedules are independently valid
- energy balance always holds
- battery rules always hold
- directives are actually applied
- end-of-day neutrality holds
- totals are independently correct
- all 10 public samples pass
- randomized/adversarial tests pass
- repeated/concurrent requests are stable
- local server works
- live deployment works
- frontend works without breaking API
- Docker works
- README is reproducible
- no secrets are exposed
- no unnecessary Claude/Anthropic references exist


FINAL REPORT:
Give me only after all work is complete:

1. Bugs discovered
2. Root causes
3. Fixes made
4. Tests added/changed
5. Full test result
6. Public samples result
7. Adversarial/randomized testing result
8. Failover testing result
9. Local verification result
10. Live deployment verification result
11. Files changed
12. Any remaining known risk

Most important instruction:

DO NOT BE A REPORTER.
BE AN INDEPENDENT BREAKER AND FIXER.

Keep searching for ways to make the system fail, and whenever you find one, fix it and test again.

FINAL RULE:
Never treat an existing feature, test, previous developer claim, or passing public sample as proof of correctness.

Whenever the official specification requires something that is missing, incomplete, inconsistent, or only partially implemented, implement the missing part yourself before finishing.

Do not leave known issues as "future work" unless the requirement is genuinely external/impossible to perform from the repository/environment.

Do not optimize for passing the visible tests; optimize for compliance with the official specification and robustness against unseen hidden tests.

Do not declare completion until the system has been independently re-audited after the final fixes.